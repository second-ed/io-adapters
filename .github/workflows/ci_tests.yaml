name: CI

on:
  pull_request:
    branches:
      - "main"
      - "dev"

jobs:
  tests:
    name: python
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - "3.11"
          - "3.14"

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install the project
        run: uv sync --dev

      - name: Run tests
        run: uv run pytest -vv --cov=src --cov-report term-missing:skip-covered --cov-fail-under=90
